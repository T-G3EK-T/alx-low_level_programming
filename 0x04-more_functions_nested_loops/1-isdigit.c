#include "main.h"
/**
 * _isdigit - do3a2
 * Description: walaboda z3ma
 * @c: Ascii code of a char
 * Return: Always 0
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
return (0);
}
